import React from "react";
import { supabase } from "../../backend/services/Supabase.ts";

const PickImage = () => {
  <>
    <div className="border-1 w-fit px-2 hover:cursor-pointer">Pick image</div>
  </>;
};

export default PickImage;
